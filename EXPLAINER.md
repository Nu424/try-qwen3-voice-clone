# Qwen3-TTS 音声クローン — 技術解説

[README.md](README.md) に書いた設定の「なぜそうなるのか」を説明する文書。
数値は原則としてこの環境（GTX 1660 Ti / RAM 15.8GB / Windows 10）での**実測値**か、
ダウンロードした `config.json` / `model.safetensors` から**直接読み取った値**。
一般論として書いている箇所はその旨を明示する。

---

## 1. 音声クローンは何をやっているのか

「声を真似る」と聞くと、参照音声から声の特徴を抽出して波形を加工する、というイメージを持つかもしれない。
Qwen3-TTS がやっているのはそれとは違い、**音声を「言語」として扱って続きを書かせる**ことに近い。

流れは3段階:

```
                    ┌─────────────────────────────────────────┐
参照音声(wav) ──────►│ speech tokenizer (encoder)              │
                    │  24kHz波形 → 離散トークン列              │
                    └─────────────────────────────────────────┘
                                    │
                                    ▼
   参照テキスト ───┐        音声トークン列
   生成テキスト ───┼───────►┌─────────────────────────────────┐
   言語ID       ───┘        │ talker (1.92B の Transformer LM)  │
                            │  「この声でこの文を読むと         │
                            │    どんなトークン列になるか」を   │
                            │    自己回帰的に予測               │
                            └─────────────────────────────────┘
                                    │
                              音声トークン列
                                    ▼
                    ┌─────────────────────────────────────────┐
                    │ speech tokenizer (decoder)              │
                    │  離散トークン列 → 24kHz波形              │
                    └─────────────────────────────────────────┘
                                    │
                                    ▼
                              生成音声(wav)
```

中核の `talker` は、テキスト生成LLMとほぼ同じ構造の Transformer。
違うのは、出力する語彙が「単語」ではなく「音声トークン」であること。

つまり音声クローンとは、**LLMのfew-shot promptingの音声版**である。
「参照テキスト＋参照音声」をプロンプトとして与えると、モデルは
「この話者・この口調」というパターンを文脈から読み取り、その続きとして
新しいテキストの音声トークンを生成する。**話者ごとの学習(fine-tuning)は一切していない**。
これが「ゼロショット音声クローン」と呼ばれる理由。

---

## 2. モデルの中身（実測）

`model.safetensors` のヘッダとconfigから読み取った実際の構成。

**総パラメータ数 1.929 B**（「1.7B」はおおよその呼称で、実測はこれより多い）

| モジュール | パラメータ数 | 役割 |
|---|---:|---|
| `talker` | 1,916.7 M | 音声トークンを生成する本体のTransformer |
| `speaker_encoder` | 12.0 M | 参照音声から話者の特徴ベクトルを取り出す |

### talker の構成

| 項目 | 値 | 補足 |
|---|---|---|
| 層数 | 28 | |
| hidden_size | 2048 | |
| attention heads | 16 | |
| KV heads | 8 | **GQA**。KVキャッシュが半分で済む |
| head_dim | 128 | |
| FFN 中間次元 | 6144 | hidden の3倍 |
| 語彙数 | 3072 | テキストLLMの15万語と比べ極端に小さい |
| max_position | 32768 | |
| RoPE theta | 1,000,000 | 長い系列に対応するための大きめの値 |

語彙数が3072しかないのは、出力するのが単語ではなく**音声コードブックのインデックス**だから
（コードブックサイズ2048 + 特殊トークン）。

### talker に付属する code predictor

| 項目 | 値 |
|---|---|
| 層数 | 5 |
| hidden_size | 1024 |
| num_code_groups | **16** |
| 語彙数 | 2048 |

これは後述する「1フレームあたり16個のコード」を出力するための小さなサブモデル。

### speech tokenizer（別ファイル 0.64 GiB）

| 項目 | 値 |
|---|---|
| 入出力サンプルレート | 24,000 Hz |
| ダウンサンプル率 | 1,920 |
| フレームレート | **12.5 Hz** |
| 量子化器の数 (RVQ) | 16 |
| コードブックサイズ | 2048（うち意味的量子化器1つは4096） |

モデル名の「12Hz」はここから来ている（正確には 24000 ÷ 1920 = 12.5 Hz）。

---

## 3. 「12Hz」が意味すること

音声波形は 24kHz なら1秒あたり24,000個の数値だが、Transformer にそのまま食わせるのは
系列が長すぎて不可能。そこで **1秒を12.5個のフレームに圧縮**する。

ただし1フレームを1個のコードで表すと情報が足りず音質が壊れる。
そこで **RVQ（Residual Vector Quantization / 残差ベクトル量子化）** を使い、
1フレームを16個のコードの重ね合わせで表現する。

```
1フレーム(80ms) = コード₁ + コード₂ + ... + コード₁₆
                   ↑粗い     ↑残差を          ↑細かい
                    輪郭      段階的に補正      ニュアンス
```

最初のコードで大まかな音を決め、次のコードでその「引き算した残り(残差)」を表現し…
を16回繰り返す。JPEGの品質段階を重ねるようなイメージ。

これで情報量は次のように圧縮される:

| | 1秒あたりの情報量 |
|---|---|
| 元の波形 (24kHz / 16bit) | 384,000 bit |
| 音声トークン (12.5 × 16 × 11bit) | **2,200 bit** |

約175分の1。この圧縮率のおかげで、33.7秒の参照音声が
`33.7 × 12.5 ≈ 421 フレーム` という Transformer が扱える長さに収まる。

**実務上の意味**: 参照音声の長さは、そのままLLMのプロンプト長として効く。
33.7秒 ≈ 421フレームは十分軽い。極端に長い参照音声（数分）を渡すと
メモリと速度の両方に効いてくる。

---

## 4. なぜ参照テキストが必要なのか

`generate_voice_clone()` は参照音声だけでなく `ref_text`（その音声の書き起こし）も要求する。
音だけあれば良さそうなのに、なぜか。

talker は「テキスト → 音声トークン」の対応関係を学習したモデルである。
参照音声だけを渡すと、モデルはそのトークン列が**何を喋っているのか**を知らないため、
「声の特徴」と「発話内容」を分離できない。

参照テキストを添えることで、プロンプトが次の形になる:

```
[この文章] → [この音声トークン列]   ← 例示（参照）
[別の文章] → ?                      ← ここを生成させる
```

これで「テキストが変わっても声は同じ」という対応をモデルが読み取れる。
**書き起こしが実際の音声とズレていると、この対応関係が壊れて品質が落ちる。**

> これが、README で「公式推奨は3〜15秒だが、書き起こしが33.7秒全体に対応しているので
> 全長をそのまま使う」と判断した理由。中途半端に切ると音声とテキストの対応が崩れ、
> 「長さが推奨内に収まる」利点を上回る害になる。

---

## 5. BF16 と FP16 — `os error 1455` の正体

今回いちばん厄介だった問題。**私は最初ここで判断を誤り、ロードに失敗した。**

### 何が起きたか

最初、こう考えた:

> GTX 1660 Ti は Turing (sm_75) 世代。bfloat16 のネイティブ対応は Ampere (sm_80) 以降。
> だから記事の `torch.bfloat16` を `torch.float16` に変えるべきだ。

そして落ちた:

```
OSError: ページング ファイルが小さすぎるため、この操作を完了できません。 (os error 1455)
```

「ページングファイル」というエラーメッセージは、一見 dtype と何の関係もないように見える。
これが原因究明を難しくしていた。

### BF16 と FP16 の違い

どちらも16bitだが、bitの配り方が違う:

| | 符号 | 指数部 | 仮数部 | 表現できる範囲 | 精度 |
|---|---|---|---|---|---|
| FP32 | 1 | 8 | 23 | ±3.4×10³⁸ | 高 |
| **BF16** | 1 | **8** | **7** | ±3.4×10³⁸（FP32と同じ） | 低 |
| **FP16** | 1 | **5** | **10** | ±65,504 | 中 |

BF16 は「FP32 の指数部をそのままに、仮数部を削ったもの」。
範囲が広いので学習中にオーバーフローしにくく、近年のモデルはほぼ BF16 で学習・配布される。

Qwen3-TTS も例外ではない。実際に `model.safetensors` のヘッダを読むと:

```
tensor dtypes: {'BF16': 480}
```

**全480テンソルが BF16**。ファイルサイズ 3.59 GiB。

### 変換コピーがメモリを食い潰す

ここで `dtype=torch.float16` を指定すると何が起きるか:

```
1. model.safetensors (3.59 GiB, BF16) をメモリマップ    → コミット 3.6 GiB
2. BF16 → FP16 に変換した新しいテンソルを作る          → コミット +3.4 GiB
                                                        ────────────────
                                                        合計 約 7 GiB
```

BF16 のままなら手順2が丸ごと不要になる。**変換先の置き場所がいらない。**

### なぜ「ページングファイル」なのか

Windows には**コミットチャージ**という概念がある。これは
「実際に使っているメモリ」ではなく「プロセスが要求した仮想メモリの総量」で、
上限は **物理RAM + ページングファイルサイズ** になる。

この環境の実測値:

| | 値 |
|---|---|
| 物理RAM | 15.8 GB |
| 空き物理RAM | 5.8 GB |
| ページングファイル | **4.5 GB**（システム管理・自動） |
| コミット上限 | 20.3 GB |
| **コミット空き** | **7.3 GB** |

必要 約7 GiB に対して空きが 7.3 GB。**ほぼ同じ**で、断片化や他プロセスの変動で
あっさり超える。そして超えた瞬間に Windows が返すエラーが `os error 1455` である。

`safe_open()` で落ちていたのは、メモリマップが「コミットの予約」を伴うため。
実際にRAMを消費する前に、要求の段階で拒否されている。

### なぜ Turing でも BF16 で動くのか

「Turing は bf16 非対応」という私の前提は、**不正確だった**。正確には:

- Turing には **bf16 のテンソルコアが無い**（＝行列積のハードウェア高速化は効かない）
- しかし PyTorch は bf16 を扱えないわけではなく、**内部で fp32 に変換して計算する**

つまり「動かない」のではなく「速度面の利点が無い」だけ。
そして今回のボトルネックは**速度ではなくメモリ**だったので、
BF16 のまま読む方が総合的に正しい選択だった。実測でも問題なく動作している。

### 教訓

GPU世代から dtype を機械的に決めるべきではない。**チェックポイントの dtype を確認し、
特段の理由が無ければそれに合わせる**のが安全。以下で確認できる:

```bash
.venv/Scripts/python.exe -c "import os,json,struct,collections; p=os.path.expanduser('~/.cache/huggingface/hub/models--Qwen--Qwen3-TTS-12Hz-1.7B-Base/snapshots/fd4b254389122332181a7c3db7f27e918eec64e3/model.safetensors'); f=open(p,'rb'); n=struct.unpack('<Q',f.read(8))[0]; h=json.loads(f.read(n)); print(collections.Counter(v['dtype'] for k,v in h.items() if k!='__metadata__'))"
```

---

## 6. FlashAttention 2 が使えない理由

こちらは dtype と違い、**本当にハードウェアの制約**。

Attention の素朴な実装は、系列長 N に対して N×N の行列を実際にメモリ上に作る。
FlashAttention は、この巨大な中間行列を**一度も書き出さずに**、
GPUの高速な SRAM 上でブロック単位に計算する手法。メモリ削減と高速化を同時に達成する。

問題は、この実装が **Ampere (sm_80) 以降の命令**に依存していること。
sm_75 の GTX 1660 Ti では、そもそもカーネルがコンパイル・実行できない。

そこで PyTorch 標準の **SDPA** (Scaled Dot Product Attention) を使う。
SDPA は複数の実装を内部に持ち、GPUに応じて自動で選ぶ:

| バックエンド | 対応 |
|---|---|
| flash attention | sm_80+ のみ |
| memory-efficient attention | **sm_75 でも使える** |
| math（素朴な実装） | どこでも動く |

sm_75 では memory-efficient 版が選ばれる。FlashAttention ほどではないが
素朴な実装より大幅に良い。**性能は落ちるが、機能的には問題なく動く。**

`scripts/voice_clone.py` はこれを compute capability から自動判定している:

```python
major, _ = torch.cuda.get_device_capability(0)
return "flash_attention_2" if major >= 8 else "sdpa"
```

Ampere 以降のGPUに載せ替えれば、コードを変えずに flash_attention_2 に切り替わる。

---

## 7. VRAM 5.16 GiB の内訳

実測ピークは 5.16 GiB / 6.0 GiB。内訳の概算:

| 項目 | 概算 | 計算根拠 |
|---|---:|---|
| talker の重み | 3.57 GiB | 1,916.7M × 2 byte |
| speaker_encoder | 0.02 GiB | 12.0M × 2 byte |
| speech tokenizer | 0.64 GiB | ファイルサイズそのまま |
| KVキャッシュ | 0.05 GiB 程度 | 下記 |
| 活性値・作業領域 | 残り | |

KVキャッシュの1トークンあたりのサイズは:

```
28層 × 8 KVヘッド × 128 head_dim × 2 (K と V) × 2 byte = 114,688 byte ≈ 0.109 MiB/token
```

GQA（KVヘッドが8で、attentionヘッド16の半分）のおかげで、
これが本来の半分に抑えられている。参照421フレーム＋生成分でも 50 MiB 程度で、
**KVキャッシュは今回のボトルネックではない**。

支配的なのは純粋に**重みの3.57 GiB**。だから軽くしたければ、
参照音声を短くするより **0.6B モデルに落とす**方が効く。

**余裕は 0.84 GiB しかない。** ブラウザやゲームなど他のGPU使用アプリが動いていると
OOM する可能性が高い。実際、実行時にも複数のアプリがGPUを使っていた。

---

## 8. なぜ生成に実時間の4倍かかるのか

実測 **RTF 4.05〜4.51**（6.2秒の音声を25〜28秒で生成）。RTF = 生成時間 ÷ 音声長。

理由は3つ:

**(1) 自己回帰生成であること**
音声トークンは1フレームずつ順番にしか作れない。6.2秒 = 約78フレームなら、
78回の推論を直列に回す必要がある。並列化できない。

**(2) メモリ帯域律速**
1フレーム生成するたびに、1.92B個の重みすべてをVRAMから読み出す。
計算量ではなく**メモリ転送**が支配的。GTX 1660 Ti のメモリ帯域は
288 GB/s 程度（一般的な公称値）で、これは近年のGPUと比べるとかなり低い。

**(3) bf16 のテンソルコアが無い**
セクション5のとおり、bf16 の演算は fp32 に落ちて実行される。

なお2回目の実行ではロードが 73.7秒 → 16.6秒 に短縮された。
これは HuggingFace のキャッシュとOSのファイルキャッシュが効いたため（ダウンロードが不要になった）。

---

## 9. 生成結果が毎回変わる理由

`generation_config.json` の実際の中身:

```json
{
  "do_sample": true,
  "temperature": 0.9,
  "top_k": 50,
  "top_p": 1.0,
  "repetition_penalty": 1.05,
  "max_new_tokens": 8192
}
```

`do_sample: true` かつ `temperature: 0.9` なので、
**毎回確率的にサンプリングされ、同じ入力でも出力が変わる**。

これは欠点ではなく、自然な抑揚を生むために必要な性質。ただし:

- **気に入った結果を再現したい** → `--seed` を指定する
- **品質が不安定** → シードを変えて複数生成し、良いものを選ぶ（現実的な運用）

```bash
for i in 1 2 3 4 5; do
  .venv/Scripts/python.exe -u scripts/voice_clone.py --seed $i --out output/take_$i.wav
done
```

---

## 10. 参照音声の要件の根拠

公式が挙げる要件と、その理由:

| 要件 | 理由 |
|---|---|
| 話者が1人だけ | speaker_encoder は音声全体から**1つ**の話者ベクトルを作る。複数話者が混ざると平均化され、どちらにも似ない声になる |
| ノイズが少ない | ノイズも「その話者の声の特徴」として学習される。BGM入りの音声で作ると、生成音声にもBGMらしき成分が乗ることがある |
| 3〜15秒 | 短すぎると特徴を捉えられない。長すぎるとプロンプトが伸びてメモリと速度に効く |
| 書き起こし必須 | セクション4のとおり |

### 今回の参照音声の分析

`reference_audio.wav` は 33.7秒 / 44.1kHz / ステレオ。ffmpeg で調べた結果:

```
mean_volume: -26.5 dB
max_volume:  -8.4 dB
```

`silencedetect` を -50dB / 0.3秒 で走らせると**無音区間が全く検出されなかった**ため、
当初は「BGM が終始鳴っている＝クローンに不向き」かと考えた。

しかし閾値を -40dB / 0.12秒 に緩めると:

```
silence_start: 8.11242  silence_end: 8.27808   (0.166秒)
silence_start: 11.7204  silence_end: 11.8584   (0.138秒)
```

短い「間」が実在した。つまり定常的なBGMではなく、**単に間の少ない早口ナレーション**だった。
音声レベルはきちんと -40dB を下回っている。クローン用途として問題ない。

前処理では **ステレオ → モノラル、44.1kHz → 24kHz** に変換している。
24kHz なのは speech tokenizer の `input_sample_rate` がその値だから。
モノラル化は、モデルが `audio_channels: 1` を前提としているため。

---

## 付録: 用語まとめ

| 用語 | 意味 |
|---|---|
| **ゼロショット音声クローン** | 対象話者で追加学習せず、参照音声をプロンプトとして与えるだけで声を真似る方式 |
| **RVQ** | 残差ベクトル量子化。粗いコードから順に残差を重ねて音声を離散表現する手法 |
| **コミットチャージ** | Windows で「プロセスが要求した仮想メモリ総量」。上限＝物理RAM＋ページングファイル |
| **compute capability (sm_XX)** | NVIDIA GPU の世代。sm_75=Turing, sm_80=Ampere, sm_89=Ada, sm_90=Hopper |
| **GQA** | Grouped Query Attention。KVヘッドを減らしてKVキャッシュを削減する手法 |
| **SDPA** | PyTorch 標準の Attention 実装。GPUに応じて最適なバックエンドを自動選択する |
| **RTF** | Real Time Factor。生成時間 ÷ 音声長。1未満ならリアルタイム合成が可能 |
